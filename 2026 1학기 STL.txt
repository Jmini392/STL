//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 3일	                                                             (1주 1일)
//------------------------------------------------------------------------------------------------------------
// VS 버전 - 17.14.27
// 컴파일 환경 - Release / x64
// 프로젝트 설정 - 일반 - C++ 언어표준 - /std::C++latest
//				 - C/C++ - SDL 설정 - 아니요
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save( const std::string& );

int main() {
	std::cout << "2026년 3월 3일" << '\n';

	save("메인.cpp");
}

void save( const std::string& fname ) {
	std::cout << "\"" << fname << "\" 저장합니다" << '\n';

	std::ifstream in{ fname };		//RAII (Resource Acquisition Is Initialization)
	if (not in) {
		std::cout << fname << " - 열 수 없습니다" << '\n';
		exit(20260303);
	}

	std::ofstream out{ "2026 1학기 STL.txt", std::ios::app };

	// in의 내용을 읽어서 out에 덧붙이자.
	// std::copy(원본, 복사본);
	std::copy( std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out} );
}

-------------------------------------------------
저장시간 - 2026-03-04 11:56:50.0391297
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 4일	                                                             (1주 2일)
//------------------------------------------------------------------------------------------------------------
// save한 시간을 기록한다
// save를 분리한다
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save( const std::string& );

int main() {
	// int t[100];
	std::array<int, 100> t; // zero-overhead
	t.size();

	std::cout << "2026년 3월 4일" << '\n';

	save("메인.cpp");
}

void save( const std::string& fname ) {
	std::cout << "\"" << fname << "\" 저장합니다" << '\n';

	std::ifstream in{ fname };		//RAII (Resource Acquisition Is Initialization)
	if (not in) {
		std::cout << fname << " - 열 수 없습니다" << '\n';
		exit(20260304);
	}

	std::ofstream out{ "2026 1학기 STL.txt", std::ios::app };

	// 저장한 시간을 out의 기록하자
	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	// 시간을 한글로 저장하자
	auto now = std::chrono::system_clock::now(); // auto는 컴파일러가 타입을 추론한다
	using namespace std::chrono_literals;
	
	out << "-------------------------------------------------" << '\n';
	out << "저장시간 - " << now + 9h << '\n';
	out << "-------------------------------------------------" << '\n';
	out << '\n';

	// in의 내용을 읽어서 out에 덧붙이자.
	// std::copy(원본, 복사본);
	std::copy( std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out} );
}

-------------------------------------------------
저장시간 - 2026-03-04 12:15:46.4157512
-------------------------------------------------

//----------------------------------------------------------------------------------------------------------
// 한 학기 강의를 저장하는 함수
// 
// 2026. 3. 4
//----------------------------------------------------------------------------------------------------------
#pragma once

void save(const std::string&);

-------------------------------------------------
저장시간 - 2026-03-04 12:15:46.4715668
-------------------------------------------------

//----------------------------------------------------------------------------------------------------------
// 한 학기 강의를 저장하는 함수
// 
// 2026. 3. 4
//----------------------------------------------------------------------------------------------------------
#include <string>
#include <iostream>
#include <fstream>
#include <chrono>
#include "save.h"

void save(const std::string& fname) {
	std::cout << "\"" << fname << "\" 저장합니다" << '\n';

	std::ifstream in{ fname };		//RAII (Resource Acquisition Is Initialization)
	if (not in) {
		std::cout << fname << " - 열 수 없습니다" << '\n';
		exit(20260304);
	}

	std::ofstream out{ "2026 1학기 STL.txt", std::ios::app };

	// 저장한 시간을 out의 기록하자
	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	// 시간을 한글로 저장하자
	auto now = std::chrono::system_clock::now(); // auto는 컴파일러가 타입을 추론한다
	using namespace std::chrono_literals;

	out << '\n';
	out << "-------------------------------------------------" << '\n';
	out << "저장시간 - " << now + 9h << '\n';
	out << "-------------------------------------------------" << '\n';
	out << '\n';

	// in의 내용을 읽어서 out에 덧붙이자.
	// std::copy(원본, 복사본);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

-------------------------------------------------
저장시간 - 2026-03-04 12:15:46.5094214
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 4일	                                                             (1주 2일)
//------------------------------------------------------------------------------------------------------------
// save한 시간을 기록한다
// save를 분리한다
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

int main() {
	std::cout << "2026년 3월 4일" << '\n';

	save("save.h");
	save("save.cpp");
	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-04 13:11:17.4644513
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 4일	                                                             (1주 2일)
//------------------------------------------------------------------------------------------------------------
// template
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"
using namespace std;

class Dog;	// 전방선언 forword declaration

void change(int&, int&);
void change(Dog&, Dog&);

class Dog {
public:
	Dog() = default;
	Dog(int n) : num{ n } {};
private:
	int num{};

	friend ostream& operator<< (ostream& os, const Dog& dog) {
		return os << dog.num;
	}
};

int main() {
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출하여 다음과 같이 출력되게 하라.	
		change(a, b);
		cout << a << ", " << b << endl; // 2, 1 출력되어야 함
	}
	
	{
		// Dog는 class로 작성하자
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출하여 다음과 같이 출력되게 하라.	
		change(a, b);
		cout << a << ", " << b << endl; // 2, 1 출력되어야 함
	}

	save("메인.cpp");
}

void change(int& a, int& b) {
	int temp{ a };
	a = b;
	b = temp;
}

void change(Dog& a, Dog& b) {
	Dog temp{ a };
	a = b;		// a.operator = (b);
	b = temp;
}


-------------------------------------------------
저장시간 - 2026-03-04 13:18:17.3945489
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 4일                                                              (1주 2일)
//------------------------------------------------------------------------------------------------------------
// template
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"
using namespace std;

class Dog;	// 전방선언 forword declaration

template <class T>
void change(T& a, T& b) {
	T temp{ a };
	a = b;		// a.operator = (b);
	b = temp;
}

class Dog {
public:
	Dog() = default;
	Dog(int n) : num{ n } {};
private:
	int num{};

	friend ostream& operator<< (ostream& os, const Dog& dog) {
		return os << dog.num;
	}
};

int main() {
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출하여 다음과 같이 출력되게 하라.	
		change(a, b);
		cout << a << ", " << b << endl; // 2, 1 출력되어야 함
	}
	
	{
		// Dog는 class로 작성하자
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출하여 다음과 같이 출력되게 하라.	
		change(a, b);
		cout << a << ", " << b << endl; // 2, 1 출력되어야 함
	}

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-10 14:21:08.5440661
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 10일                                                             (2주 1일)
//------------------------------------------------------------------------------------------------------------
// template
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"
using namespace std;

class Dog;	// 전방선언 forword declaration

// compiler phase

template <class T>
void change(T& a, T& b) {
	T temp{ a };
	a = b;		// a.operator = (b);
	b = temp;
}

class Dog {
public:
	Dog() = default;
	Dog(int n) : num{ n } {};
private:
	int num{};

	friend ostream& operator<< (ostream& os, const Dog& dog) {
		return os << dog.num;
	}
};

template <>
void change<Dog>(Dog& a, Dog& b) {
	Dog temp{ a };
	a = b;		// a.operator = (b);
	b = temp;
}

int main() {
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출하여 다음과 같이 출력되게 하라.	
		change(a, b);
		cout << a << ", " << b << endl; // 2, 1 출력되어야 함
	}
	
	{
		// Dog는 class로 작성하자
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출하여 다음과 같이 출력되게 하라.	
		change(a, b);
		cout << a << ", " << b << endl; // 2, 1 출력되어야 함
	}

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-10 14:25:34.2018353
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 10일                                                             (2주 1일)
//------------------------------------------------------------------------------------------------------------
// template
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"
using namespace std;

class Dog;	// 전방선언 forword declaration

// compiler phase

template <class T>
void change(T& a, T& b) {
	T temp{ a };
	a = b;		// a.operator = (b);
	b = temp;
}

class Dog {
public:
	Dog() = default;
	Dog(int n) : num{ n } {};
private:
	int num{};

	friend ostream& operator<< (ostream& os, const Dog& dog) {
		return os << dog.num;
	}
};

template <>				// specialization
void change<Dog>(Dog& a, Dog& b) {
	cout << "멍멍" << endl;
}

int main() {
	{
		int a{ 1 }, b{ 2 };
		change(a, b);
		cout << a << ", " << b << endl; // 2, 1 출력되어야 함
	}
	
	{
		Dog a{ 1 }, b{ 2 };				// 함수 호출 우선순위
		change(a, b);
		cout << a << ", " << b << endl; // 2, 1 출력되어야 함
	}

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-10 14:40:22.5181385
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 10일                                                             (2주 1일)
//------------------------------------------------------------------------------------------------------------
// template
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"
using namespace std;

class Dog;	// 전방선언 forword declaration

// compiler phase

template <class T>
void change(T& a, T& b) {
	T temp{ a };
	a = b;		// a.operator = (b);
	b = temp;
}

class Dog {
public:
	Dog() = default;
	Dog(int n) : num{ n } {};
private:
	int num{};

	friend ostream& operator<< (ostream& os, const Dog& dog) {
		return os << dog.num;
	}
};

void change(Dog& a, Dog& b) {
	cout << "나는 전역(Global) 함수야" << endl;
}

template <>				// specialization
void change<Dog>(Dog& a, Dog& b) {
	cout << "멍멍" << endl;
}

int main() {
	{
		int a{ 1 }, b{ 2 };
		change(a, b);
		cout << a << ", " << b << endl; // 2, 1 출력되어야 함
	}
	
	{
		Dog a{ 1 }, b{ 2 };				// 함수 호출 우선순위
		change(a, b);
		cout << a << ", " << b << endl; // 2, 1 출력되어야 함
	}

	save("메인.cpp");
}

-------------------------------------------------
저장시간 - 2026-03-10 14:52:40.7514493
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 10일                                                             (2주 1일)
//------------------------------------------------------------------------------------------------------------
// template
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"
using namespace std;

template <class T>
T add(const T& a, const T& b) {
	return a + b;	// RVO 
}

int main() {
	// [문제] template함수 add로 실행되게 하자
	cout << add(2026, 310) << endl;
	cout << add("2026년 "s, "3월 10일"s) << endl;

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-10 15:00:44.5672297
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 10일                                                             (2주 1일)
//------------------------------------------------------------------------------------------------------------
// C++ 언어에서 template의 역할은? -> Generic Programming의 핵심 keyword
// 
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"
using namespace std;

int main() {
	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-10 15:12:07.8772926
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 10일                                                             (2주 1일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"
using namespace std;

default_random_engine dre;
uniform_int_distribution<int> uid;

int main() {
	// [문제] 랜덤값을 갖는 int 1000개를 생성하라. 모든 값을 화면에 출력하라.
	for (int i = 0; i < 1000; i++) {
		cout << uid(dre) << endl;
	}
			
	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-10 15:13:35.6873762
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 10일                                                             (2주 1일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"
using namespace std;

default_random_engine dre;
uniform_int_distribution<int> uid{ numeric_limits<int>::min(), 2147283647 };

int main() {
	// [문제] 랜덤값을 갖는 int 1000개를 생성하라. 모든 값을 화면에 출력하라.
	for (int i = 0; i < 1000; i++) {
		cout << uid(dre) << endl;
	}
			
	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-10 15:16:30.1879184
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 10일                                                             (2주 1일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"
using namespace std;

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999 };

int main() {
	// [문제] 랜덤값을 갖는 int 1000개를 생성하라. 모든 값을 화면에 출력하라.
	int num[1000];
	for (int i = 0; i < 1000; i++) {
		num[i] = uid(dre);
	}

	for (int num : num) {
		cout << num << " ";
	}
			
	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-10 15:18:32.3960539
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 10일                                                             (2주 1일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include "save.h"
using namespace std;

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999 };

int main() {
	// [문제] 랜덤값을 갖는 int 1000개를 생성하라. 모든 값을 화면에 출력하라.
	int num[1000];
	for (int i = 0; i < 1000; i++) {
		num[i] = uid(dre);
	}

	for (int num : num) {
		print("{:8}", num);
	}
			
	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-11 11:41:05.8040896
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 11일                                                             (2주 2일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include "save.h"
using namespace std;

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999 };

int main() {
	// [문제] 랜덤값을 갖는 int 1000개를 생성하라. 모든 값을 화면에 출력하라.
	int num[1000];
	for (int i = 0; i < 1000; i++) {
		num[i] = uid(dre);
	}

	for (int num : num) {
		print("{:8}", num);
	}

	// [문제] 가장 큰 값을 찾아 화면에 출력하자
	int MaxVal = numeric_limits<int>::min();

	for (int n : num) {
		if (n > MaxVal) {
			MaxVal = n;
		}
	}
	
	cout << "가장 큰 값은 - " << MaxVal << endl;
			
	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-11 12:03:40.3061216
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 11일                                                             (2주 2일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>
#include "save.h"
using namespace std;

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999 };

int main() {
	int num[1000];
	for (int i = 0; i < 1000; i++) {
		num[i] = uid(dre);
	}

	for (int num : num) {
		print("{:8}", num);
	}

	// [문제] 가장 큰 값을 찾아 화면에 출력하자
	auto MaxValPos = max_element(begin(num), end(num));
	cout << "가장 큰 값 - " << *MaxValPos << endl;

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-11 12:05:03.8343455
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 11일                                                             (2주 2일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>
#include "save.h"
using namespace std;

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999 };

int main() {
	int num[1000];
	for (int i = 0; i < 1000; i++) {
		num[i] = uid(dre);
	}

	for (int num : num) {
		print("{:8}", num);
	}

	// [문제] 가장 작은 값을 찾아 화면에 출력하자
	auto MinValPos = min_element(begin(num), end(num));
	cout << "가장 작은 값 - " << *MinValPos << endl;

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-11 12:10:59.1064362
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 11일                                                             (2주 2일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>
#include "save.h"
using namespace std;

default_random_engine dre{ random_device{}() };
uniform_int_distribution<int> uid{ 0, 9999 };

int main() {
	int num[1000];
	for (int i = 0; i < 1000; i++) {
		num[i] = uid(dre);
	}

	for (int num : num) {
		print("{:8}", num);
	}

	// [문제] 가장 작은 값을 찾아 화면에 출력하자
	auto MinValPos = min_element(begin(num), end(num));
	cout << "가장 작은 값 - " << *MinValPos << endl;

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-11 12:24:14.5048092
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 11일                                                             (2주 2일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>
#include "save.h"
using namespace std;

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999 };

int main() {
	int num[1000];
	for (int i = 0; i < 1000; i++) {
		num[i] = uid(dre);
	}

	for (int num : num) {
		print("{:8}", num);
	}

	// [문제] 가장 큰 값과 가장 작은 값을 찾아 화면에 출력하자
	auto Val = minmax_element(begin(num), end(num));
	cout << "가장 작은 값 - " << *Val.first << "가장 큰 값 - " << *Val.second << endl;

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-11 12:26:22.7100868
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 11일                                                             (2주 2일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>
#include "save.h"
using namespace std;

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999 };

int main() {
	int num[1000];
	for (int i = 0; i < 1000; i++) {
		num[i] = uid(dre);
	}

	for (int num : num) {
		print("{:8}", num);
	}

	// [문제] 가장 큰 값과 가장 작은 값을 찾아 화면에 출력하자
	// structured binding - syntactic sugar
	auto [MinVal, MaxVal] = minmax_element(begin(num), end(num));
	cout << "가장 작은 값 - " << *MinVal << "가장 큰 값 - " << *MaxVal << endl;

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-11 12:54:36.9429418
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 11일                                                             (2주 2일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>
#include <array>
#include "save.h"
using namespace std;

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999 };

template <class T, int N>
class myArray {
public:
	T& operator[](int idx) {
		return arr[idx];
	}
	size_t size() { 
		return N;
	}
	T* begin() {
		return &arr[0];
	}
	T* end() {
		return &arr[N];
	}
	
private:
	T arr[N];
};

int main() {
	// int num[1000];				// 이 자료구조는 사용하지 말자
	myArray<int, 1000> num;

	for (int i = 0; i < num.size(); i++) {
		num[i] = uid(dre);
	}

	for (int num : num) {
		print("{:8}", num);
	}

	// [문제] 가장 큰 값과 가장 작은 값을 찾아 화면에 출력하자
	// structured binding - syntactic sugar
	auto [MinVal, MaxVal] = minmax_element(num.begin(), num.end());
	cout << "가장 작은 값 - " << *MinVal << endl << "가장 큰 값 - " << *MaxVal << endl;

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-11 13:11:27.8410663
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 11일                                                             (2주 2일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <fstream>
#include "save.h"
using namespace std;

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 999'9999 };

int main() {
	// [문제] 랜덤 int값 1000만개를 "int값1000만개.txt"에 저장하라
	ofstream out{ "int값1000만개.txt"};
	for (int i = 0; i < 1000'0000; ++i) {
		out << uid(dre) << ' ';
	}

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-17 13:46:37.0369952
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 17일                                                             (3주 1일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <fstream>
#include "save.h"
using namespace std;

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 999'9999 };

int main() {
	// [문제] text모드로 저장된 "int값1000만개.txt"에 저장되어 있는 int값중에서
	// 가장 작은 값을 찾아 출력하라. int는 천만개가 확실해 - 믿어라.
	
	// 1. 몇 개인지는 읽기 전에 알 수 없다.
	// 2. 파일 크기로부터 몇 개인지 알 수 없다.

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-17 14:00:25.0893138
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 17일                                                             (3주 1일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <array>
#include "save.h"
using namespace std;

array<int, 1000'0000 > a;

int main() {
	// [문제] text모드로 저장된 "int값1000만개.txt"에 저장되어 있는 int값중에서
	// 가장 작은 값을 찾아 출력하라. int는 천만개가 확실해 - 믿어라.
	
	ifstream in{ "int값1000만개.txt" };
	if (not in) {
		cout << "파일에 뭔가 문제가..." << endl;
		return 20260317;
	}
	
	size_t cnt{};
	for (int& num : a) {
		in >> num;
		++cnt;
	}
	cout << "읽은 개수 - " << cnt << endl;

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-17 14:04:29.3549415
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 17일                                                             (3주 1일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <array>
#include <algorithm>
#include "save.h"
using namespace std;

array<int, 1000'0000 > a;

int main() {
	// [문제] text모드로 저장된 "int값1000만개.txt"에 저장되어 있는 int값중에서
	// 가장 작은 값을 찾아 출력하라. int는 천만개가 확실해 - 믿어라.
	
	ifstream in{ "int값1000만개.txt" };
	if (not in) {
		cout << "파일에 뭔가 문제가..." << endl;
		return 20260317;
	}
	
	for (int& num : a) {
		in >> num;
	}

	cout << "최솟값 - " << *min_element(a.begin(), a.end()) << endl;

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-17 14:12:01.9928195
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 17일                                                             (3주 1일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <array>
#include <algorithm>
#include "save.h"
using namespace std;

int main() {
	// [문제] text모드로 저장된 "int값1000만개.txt"에 저장되어 있는 int값중에서
	// 가장 작은 값을 찾아 출력하라. int는 천만개가 확실해 - 믿어라.
	
	ifstream in{ "int값1000만개.txt" };
	if (not in) {
		cout << "파일에 뭔가 문제가..." << endl;
		return 20260317;
	}

	cout << "최솟값 - " << *min_element(istream_iterator<int>{in}, {}) << endl;
	
	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-17 14:12:45.2198010
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 17일                                                             (3주 1일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <array>
#include <algorithm>
#include "save.h"
using namespace std;

int main() {
	// [문제] text모드로 저장된 "int값1000만개.txt"에 저장되어 있는 int값중에서
	// 가장 큰 값을 찾아 출력하라. int는 천만개가 확실해 - 믿어라.
	
	ifstream in{ "int값1000만개.txt" };
	if (not in) {
		cout << "파일에 뭔가 문제가..." << endl;
		return 20260317;
	}

	cout << "최댓값 - " << *max_element(istream_iterator<int>{in}, {}) << endl;
	
	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-17 14:27:01.3173128
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 17일                                                             (3주 1일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <array>
#include <algorithm>
#include "save.h"
using namespace std;

int main() {
	// [문제] "int값1000만개.txt" 파일은 int 1000만개를 저장하는데 77MB 이상을 사용한다.
	// int 1000만개를 저장하는데 필요한 메모리는 몇 바이트인가?
	// 1000만 * sizeof(int) = 40000000바이트 ==> 40MB에서 한 바이트도 가감되지 않음

	// little-endian
	
	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-17 14:31:18.6318197
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 17일                                                             (3주 1일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <array>
#include <algorithm>
#include "save.h"
using namespace std;

int main() {
	// [문제] int로 표현 가능한 모든 수를 파일에 text로 저장하였다. 수와 수는 한 개의 공백 문자로 분리하였다.
	// int 한 개를 저장하기 위한 평균 바이트를 계산하라.
	
	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-17 14:46:37.7524898
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 17일                                                             (3주 1일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include "save.h"
using namespace std;

int main() {
	// [문제] int 1억개를 파일에 저장. - 걸린 시간
	ofstream out{ "int억개" };
	for (int i = 0; i < 1'0000'0000; ++i) {
		out << i << " ";
	}
	
	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-17 14:58:13.6075709
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 17일                                                             (3주 1일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include "save.h"
using namespace std;

int main() {
	// [문제] int 1억개를 binary 모드로 파일에 저장. - 걸린 시간 관찰
	// 텍스트와 바이너리 모드의 차이점은?

	ofstream out{ "int억개", ios::binary };
	for (int i = 0; i < 1'0000'0000; ++i) {
		out.write(reinterpret_cast<char*>(&i), sizeof(int));
		// out.write(어디를, 몇개나?)	// DMA(Direct Memory Access)
	}
	
	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-17 15:04:46.2520200
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 17일                                                             (3주 1일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <array>
#include <numeric>
#include <print>
#include "save.h"
using namespace std;

int main() {
	// [문제] int 1000개를 다음과 같이 binary 모드로 파일에 기록하였다.
	array<int, 1000> a;
	
	iota(a.begin(), a.end(), 1);

	for (int num : a) {
		print("{:8}", num);
	}

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-17 15:08:55.9994635
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 17일                                                             (3주 1일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <array>
#include <numeric>
#include <print>
#include "save.h"
using namespace std;

int main() {
	// [문제] int 1000개를 다음과 같이 binary 모드로 파일 "int 천개.bin"에 기록하였다.
	array<int, 1000> a;
	
	iota(a.begin(), a.end(), 1);

	ofstream out{ "int 천개.bin", ios::binary };
	out.write(reinterpret_cast<char*>(&a[0]), a.size() * sizeof(int));

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-18 11:33:32.6804062
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 18일                                                             (3주 2일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <array>
#include <numeric>
#include <print>
#include "save.h"
using namespace std;

int main() {
	// [문제] "int 천개.bin"에는 1000개의 int값이 저장되어있다.
	// 파일은 binary mode
	// 메모리에 int 1000개 값을 읽어 오세요.

	ifstream in("int 천개.bin", ios::binary);
	if (not in) {
		cout << "" << endl;
		return 20260317;
	}

	array<int, 1000> a;
	in.read(reinterpret_cast<char*>(&a[0]), a.size() * sizeof(int));

	for (int num : a) {
		cout << num << " ";
	}

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-18 11:57:01.0535785
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 18일                                                             (3주 2일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <string>
#include <array>
#include "save.h"
using namespace std;

default_random_engine dre;
uniform_int_distribution uid{ 0, 9999 };
uniform_int_distribution uidNameLen{ 10, 30 };
uniform_int_distribution<int> uidChar{ 'a', 'z' };

class Dog {
public:
	Dog() {
		num = uid(dre);
		size_t len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
private:
	string name;				// 10 이상 30 이하 임의의 소문자
	int num;

	friend ostream& operator<<(ostream& os, const Dog& dog) {
		print("[{:4}] - {} ", dog.num, dog.name);
		return os;
	}
};

int main() {
	for (int i = 0; i < 10; ++i) {
		cout << Dog() << endl;
	}

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-18 11:58:49.4469412
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 18일                                                             (3주 2일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <string>
#include <array>
#include "save.h"
using namespace std;

default_random_engine dre;
uniform_int_distribution uid{ 0, 9999 };
uniform_int_distribution uidNameLen{ 10, 30 };
uniform_int_distribution<int> uidChar{ 'a', 'z' };

class Dog {
public:
	Dog() {
		num = uid(dre);
		size_t len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
private:
	string name;				// 10 이상 30 이하 임의의 소문자
	int num;

	friend ostream& operator<<(ostream& os, const Dog& dog) {
		print("[{:4}] - {} ", dog.num, dog.name);
		return os;
	}
};

int main() {
	// [문제] Dog 100마리를 메모리에 저장하라.
	array<Dog, 100> dogs;

	for (const Dog& dog : dogs) {
		cout << dog << endl;
	}

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-18 12:11:04.1775077
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 18일                                                             (3주 2일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <string>
#include <array>
#include <fstream>
#include "save.h"
using namespace std;

default_random_engine dre;
uniform_int_distribution uid{ 0, 9999 };
uniform_int_distribution uidNameLen{ 10, 30 };
uniform_int_distribution<int> uidChar{ 'a', 'z' };

class Dog {
public:
	Dog() {
		num = uid(dre);
		size_t len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
private:
	string name;				// 10 이상 30 이하 임의의 소문자
	int num;

	friend ostream& operator<<(ostream& os, const Dog& dog) {
		print("[{:4}] - {} ", dog.num, dog.name);
		return os;
	}
};

int main() {
	array<Dog, 100> dogs;

	// [문제] dogs를 파일 "Dog백마리"에 저장하라.
	// 파일 크기는 size(Dog) * dogs.size() 이어야 한다.

	ofstream out{ "Dog백마리", ios::binary };
	out.write(reinterpret_cast<char*>(dogs.data()), sizeof(Dog) * dogs.size());

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-18 12:51:31.9054245
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 18일                                                             (3주 2일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <print>
#include <string>
#include <fstream>
#include <array>
#include <filesystem>
#include "save.h"
using namespace std;

class Dog {
private:
	string name;				// 10 이상 30 이하 임의의 소문자
	int num;

	friend ostream& operator<<(ostream& os, const Dog& dog) {
		print("[{:4}] - {} ", dog.num, dog.name);
		return os;
	}
};

int main() {
	// [문제] binary 모드로 저장한 파일 "Dog백마리"에는 Dog 객체가 저장되어있다.
	// Dog 객체는 파일의 write 함수를 사용하여 객체 크기 그대로 저장하였다.

	// 몇 객체인지 확인할 수 있는 방법?
	// 1. 파일사이즈 / sizeof Dog
	// 2. 직접 읽어 몇 개 인지 센다.

	ifstream in{ "Dog백마리", ios::binary };

	cout << "Dog의 개수 - " << filesystem::file_size("Dog백마리") / sizeof(Dog) << endl;
	
	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-18 12:54:15.0349270
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 18일                                                             (3주 2일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <print>
#include <string>
#include <fstream>
#include <array>
#include <filesystem>
#include "save.h"
using namespace std;

class Dog {
private:
	string name;				// 10 이상 30 이하 임의의 소문자
	int num;

	friend ostream& operator<<(ostream& os, const Dog& dog) {
		print("[{:4}] - {} ", dog.num, dog.name);
		return os;
	}
};

int main() {
	// [문제] binary 모드로 저장한 파일 "Dog백마리"에는 Dog 객체가 저장되어있다.
	// Dog 객체는 파일의 write 함수를 사용하여 객체 크기 그대로 저장하였다.

	// 몇 객체인지 확인할 수 있는 방법?
	// 1. 파일사이즈 / sizeof Dog
	// 2. 직접 읽어 몇 개 인지 센다.

	ifstream in{ "Dog백마리", ios::binary };

	Dog dog;
	size_t cnt{};
	while (in.read(reinterpret_cast<char*>(&dog), sizeof Dog)) {
		++cnt;
	}
	cout << "Dog의 개수 - " << cnt << endl;

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-18 12:57:31.9068744
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 18일                                                             (3주 2일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <print>
#include <string>
#include <fstream>
#include <array>
#include <filesystem>
#include "save.h"
using namespace std;

class Dog {
private:
	string name;				// 10 이상 30 이하 임의의 소문자
	int num;

	friend ostream& operator<<(ostream& os, const Dog& dog) {
		print("[{:4}] - {} ", dog.num, dog.name);
		return os;
	}
	friend istream& operator>>(istream& is, Dog& dog) {
		is.read((char*)&dog, sizeof(Dog));
		return is;
	}
};

int main() {
	// [문제] binary 모드로 저장한 파일 "Dog백마리"에는 Dog 객체가 저장되어있다.
	// Dog 객체는 파일의 write 함수를 사용하여 객체 크기 그대로 저장하였다.

	// 몇 객체인지 확인할 수 있는 방법?
	// 1. 파일사이즈 / sizeof Dog
	// 2. 직접 읽어 몇 개 인지 센다.

	ifstream in{ "Dog백마리", ios::binary };

	Dog dog;
	size_t cnt{};
	while (in >> dog) {
		++cnt;
	}
	cout << "Dog의 개수 - " << cnt << endl;

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-18 13:02:34.9521783
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 18일                                                             (3주 2일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <print>
#include <string>
#include <fstream>
#include <array>
#include <filesystem>
#include "save.h"
using namespace std;

class Dog {
private:
	string name;				// 10 이상 30 이하 임의의 소문자
	int num;

	friend ostream& operator<<(ostream& os, const Dog& dog) {
		print("[{:4}] - {} ", dog.num, dog.name);
		return os;
	}
	friend istream& operator>>(istream& is, Dog& dog) {
		is.read((char*)&dog, sizeof(Dog));
		return is;
	}
};

int main() {
	string s{ "1234567890" };

	ofstream out{ "표준 string저장", ios::binary };
	out.write((char*)&s, sizeof(string));

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-18 13:04:40.5542184
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 18일                                                             (3주 2일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <print>
#include <string>
#include <fstream>
#include <array>
#include <filesystem>
#include "save.h"
using namespace std;

class Dog {
private:
	string name;				// 10 이상 30 이하 임의의 소문자
	int num;

	friend ostream& operator<<(ostream& os, const Dog& dog) {
		print("[{:4}] - {} ", dog.num, dog.name);
		return os;
	}
	friend istream& operator>>(istream& is, Dog& dog) {
		is.read((char*)&dog, sizeof(Dog));
		return is;
	}
};

int main() {
	ifstream in{ "표준 string저장", ios::binary };

	string s;

	in.read((char*)&s, sizeof(string));

	cout << s << endl;

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-03-18 13:20:34.2827982
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		3월 18일                                                             (3주 2일)
//------------------------------------------------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <string>
#include <array>
#include <fstream>
#include <algorithm>
#include "save.h"
using namespace std;

default_random_engine dre;
uniform_int_distribution uid{ 0, 9999 };
uniform_int_distribution uidNameLen{ 10, 30 };
uniform_int_distribution<int> uidChar{ 'a', 'z' };

class Dog {
public:
	Dog() {
		num = uid(dre);
		size_t len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
	string getName() const { return name; }
private:
	string name;				// 10 이상 30 이하 임의의 소문자
	int num;

	friend ostream& operator<<(ostream& os, const Dog& dog) {
		print("[{:4}] - {} ", dog.num, dog.name);
		return os;
	}
};

int main() {
	array<Dog, 1000> dogs;

	// 이름 순으로 정렬하라
	sort(dogs.begin(), dogs.end(), [](const Dog& a, const Dog& b) {
		return a.getName() < b.getName();
		});

	for (const Dog& dog : dogs) {
		cout << dog << endl;
	}

	save("메인.cpp");
}

