
-------------------------------------------------
저장시간 - 2026-04-28 14:07:30.1178560
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 28일                                                             (9주 1일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	vector<int> v{ 1, 2, 3, 4, 5 };

	// [문제] v에서 3을 제거하라.
	// v.size() == 4
	// v.capacity() == 5
	// v.data() 동일
	// v의 원소 {1, 2, 4, 5}

	// std::remove를 사용하면 된다
	remove(v.begin(), v.end(), 3);
	// OCP(Open-Closed Principle)

	for (int i : v) {
		cout << i << ' ';
	}
	cout << endl;
	
	// design pattern 자료구조 -> (iterator) -> 알고리즘
}


-------------------------------------------------
저장시간 - 2026-04-28 15:00:46.8549343
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 28일                                                             (9주 1일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	vector<int> v{ 1, 2, 3, 4, 5 };

	// std::remove를 사용하면 된다
	vector<int>::iterator newEnd = remove(v.begin(), v.end(), 3);
	v.erase(newEnd, v.end());

	for (int i : v) {
		cout << i << ' ';
	}
	cout << endl;
}


-------------------------------------------------
저장시간 - 2026-04-28 15:02:16.1826138
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 28일                                                             (9주 1일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	vector<int> v{ 1, 2, 3, 4, 5 };

	// std::remove를 사용하면 된다
	v.erase(remove(v.begin(), v.end(), 3), v.end()); // remove-erase idiom

	for (int i : v) {
		cout << i << ' ';
	}
	cout << endl;
}


-------------------------------------------------
저장시간 - 2026-04-28 15:04:23.4154682
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 28일                                                             (9주 1일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	vector<int> v{ 1, 2, 3, 4, 5 };

	// std::remove를 사용하면 된다
	// C++20부터
	erase(v, 3);

	for (int i : v) {
		cout << i << ' ';
	}
	cout << endl;
}


-------------------------------------------------
저장시간 - 2026-04-28 15:18:20.8496209
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 28일                                                             (9주 1일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	vector<YString> v{ "1", "22", "333" };

	// [문제] v에서 "22"를 제거하라
	remove(v.begin(), v.end(), "22");

	for (const YString& s : v)
		cout << s << endl;
}


-------------------------------------------------
저장시간 - 2026-04-28 15:20:43.4926020
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 28일                                                             (9주 1일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	vector<YString> v{ "1", "22", "333" };

	// [문제] v에서 "22"를 제거하라
	erase(v, "22");

	for (const YString& s : v)
		cout << s << endl;
}


-------------------------------------------------
저장시간 - 2026-04-29 11:33:13.9259573
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 29일                                                             (9주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	//save("메인.cpp");

	vector<YString> v{ "1", "22", "333", "44", "55555", "66"};

	// [문제] v에서 길이가 2인 element를 다 삭제해주세요

	erase(v, [](const YString& s) { return s.Getlen() == 2; });

	for (const YString& s : v)
		cout << s << " ";

}


-------------------------------------------------
저장시간 - 2026-04-29 11:33:34.2592627
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 29일                                                             (9주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	//save("메인.cpp");

	vector<YString> v{ "1", "22", "333", "44", "55555", "66"};

	// [문제] v에서 길이가 2인 element를 다 삭제해주세요

	erase(v, [](const YString& s) { return s.Getlen() == 2; });

	for (const YString& s : v)
		cout << s << " ";

}


-------------------------------------------------
저장시간 - 2026-04-29 11:34:17.7824643
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 29일                                                             (9주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	//save("메인.cpp");

	//vector<YString> v{ "1", "22", "333", "44", "55555", "66"};
	vector<string> v{ "1", "22", "333", "44", "55555", "66"};

	// [문제] v에서 길이가 2인 element를 다 삭제해주세요

	erase(v, [](const string& s) { return s.length() == 2; });

	for (const string& s : v)
		cout << s << " ";

}


-------------------------------------------------
저장시간 - 2026-04-29 11:41:29.5257457
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 29일                                                             (9주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	vector<YString> v{ "1", "22", "333", "44", "55555", "66"};

	// [문제] v에서 길이가 2인 element를 다 삭제해주세요

	remove_if(v.begin(), v.end(), [](const YString& ys) {
		if (ys.Getlen() == 2) return true;
		return false;
		});
	
	for (const YString& s : v)
		cout << s << " ";

}


-------------------------------------------------
저장시간 - 2026-04-29 11:41:44.0121276
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 29일                                                             (9주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	vector<YString> v{ "1", "22", "333", "44", "55555", "66"};

	// [문제] v에서 길이가 2인 element를 다 삭제해주세요

	auto newEnd = remove_if(v.begin(), v.end(), [](const YString& ys) {
		if (ys.Getlen() == 2) return true;
		return false;
		});
	v.erase(newEnd, v.end());

	for (const YString& s : v)
		cout << s << " ";

}


-------------------------------------------------
저장시간 - 2026-04-29 11:42:35.5663966
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 29일                                                             (9주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	vector<YString> v{ "1", "22", "333", "44", "55555", "66"};

	// [문제] v에서 길이가 2인 element를 다 삭제해주세요

	erase_if(v, [](const YString& ys) {
		if (ys.Getlen() == 2) return true;
		return false;
		});

	for (const YString& s : v)
		cout << s << " ";

}


-------------------------------------------------
저장시간 - 2026-04-29 11:50:12.6251291
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 29일                                                             (9주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	vector<YString> v{ "1", "22", "44", "55555"};

	// [문제] "22" 다음에 "333"을 추가하라

	v.insert(v.begin() + 2, "333");

	for (const YString& s : v)
		cout << s << " ";

}


-------------------------------------------------
저장시간 - 2026-04-29 11:56:31.6819706
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 29일                                                             (9주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	list<YString> v{ "1", "22", "44", "55555"};

	// [문제] "22" 다음에 "333"을 추가하라

	관찰 = true;
	auto i = v.begin();
	advance(i, 2);
	v.insert(i, "333");
	관찰 = false;

	for (const YString& s : v)
		cout << s << " ";
}


-------------------------------------------------
저장시간 - 2026-04-29 11:57:46.5880573
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 29일                                                             (9주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	list<YString> v{ "1", "22", "44", "55555"};

	// [문제] "22" 다음에 "333"을 추가하라

	관찰 = true;
	auto i = v.begin();
	advance(i, 2);
	v.emplace(i, "333");
	관찰 = false;

	for (const YString& s : v)
		cout << s << " ";
}


-------------------------------------------------
저장시간 - 2026-04-29 11:58:49.0728163
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 29일                                                             (9주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	vector<YString> v{ "1", "22", "44", "55555"};

	// [문제] "22" 다음에 "333"을 추가하라

	관찰 = true;
	auto i = v.begin();
	advance(i, 2);
	v.emplace(i, "333");
	관찰 = false;

	for (const YString& s : v)
		cout << s << " ";
}


-------------------------------------------------
저장시간 - 2026-04-29 12:00:28.8943485
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 29일                                                             (9주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	vector<YString> v{ "1", "22", "44", "55555"};

	// [문제] "22" 다음에 "333"을 추가하라

	관찰 = true;
	v.emplace(++ ++v.begin(), "333");
	관찰 = false;

	for (const YString& s : v)
		cout << s << " ";
}


-------------------------------------------------
저장시간 - 2026-04-29 12:52:02.9602510
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 29일                                                             (9주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array -> random access(임의의 위치에 접근이 O(1))
// list<T> - 임의의 위치에서 추가/제거 동작이 O(1)
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <list>
#include <algorithm>
#include <ranges>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	list<YString> c{ "333", "1", "55555", "4444", "22" };

	// [문제] 길이 기준 오름차순으로 정렬하라
	c.sort([](const YString& a, const YString& b) {
		return a.size() < b.size();
		});

	for (const YString& ys : c)
		cout << ys << endl;
}


-------------------------------------------------
저장시간 - 2026-04-29 12:52:43.8088306
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 29일                                                             (9주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array -> random access(임의의 위치에 접근이 O(1))
// list<T> - 임의의 위치에서 추가/제거 동작이 O(1)
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <list>
#include <algorithm>
#include <ranges>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	list<YString> c{ "333", "1", "55555", "4444", "22" };

	// [문제] 길이 기준 오름차순으로 정렬하라
	관찰 = true;
	c.sort([](const YString& a, const YString& b) {
		return a.size() < b.size();
		});
	관찰 = false;

	for (const YString& ys : c)
		cout << ys << endl;
}


-------------------------------------------------
저장시간 - 2026-04-29 13:07:58.7112800
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 29일                                                             (9주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array -> random access(임의의 위치에 접근이 O(1))
// list<T> - 임의의 위치에서 추가/제거 동작이 O(1)
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <list>
#include <algorithm>
#include <fstream>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	// [문제] 파일에 있는 단어를 list에 저장하라.
	// list를 사전기준으로 오름차순 정렬하라.
	// 화면에 출력하라

	ifstream in{ "2026 1학기 STL.txt" };
	if (not in) return 20260429;

	cout << "파일 읽는중";
	list<YString> cont{ istream_iterator<YString>{in}, {} };
	cout << endl;

	for (const YString& e : cont) {
		cout << e << endl;
	}
}


-------------------------------------------------
저장시간 - 2026-04-29 13:11:15.8882558
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 29일                                                             (9주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array -> random access(임의의 위치에 접근이 O(1))
// list<T> - 임의의 위치에서 추가/제거 동작이 O(1)
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <list>
#include <algorithm>
#include <fstream>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	// [문제] 파일에 있는 단어를 list에 저장하라.
	// list를 사전기준으로 오름차순 정렬하라.
	// 화면에 출력하라

	ifstream in{ "2026 1학기 STL.txt" };
	if (not in) return 20260429;

	cout << "파일 읽는중";
	list<YString> cont{ istream_iterator<YString>{in}, {} };
	cout << endl;

	관찰 = true;
	for (const YString& e : cont) {
		e.show();
	}
	관찰 = false;
}


-------------------------------------------------
저장시간 - 2026-04-29 13:12:21.9923556
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 29일                                                             (9주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array -> random access(임의의 위치에 접근이 O(1))
// list<T> - 임의의 위치에서 추가/제거 동작이 O(1)
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <list>
#include <algorithm>
#include <fstream>
#include <vector>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	// [문제] 파일에 있는 단어를 list에 저장하라.
	// list를 사전기준으로 오름차순 정렬하라.
	// 화면에 출력하라

	ifstream in{ "2026 1학기 STL.txt" };
	if (not in) return 20260429;

	cout << "파일 읽는중";
	vector<YString> cont{ istream_iterator<YString>{in}, {} };
	cout << endl;

	관찰 = true;
	for (const YString& e : cont) {
		e.show();
	}
	관찰 = false;
}


-------------------------------------------------
저장시간 - 2026-04-29 13:18:44.5001533
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		4월 29일                                                             (9주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array -> random access(임의의 위치에 접근이 O(1))
// list<T> - 임의의 위치에서 추가/제거 동작이 O(1)
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <list>
#include <algorithm>
#include <fstream>
#include <vector>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	// [문제] 파일에 있는 단어를 list에 저장하라.
	// list를 사전기준으로 오름차순 정렬하라.
	// 화면에 출력하라

	ifstream in{ "2026 1학기 STL.txt" };
	if (not in) return 20260429;

	cout << "파일 읽는중";
	list<YString> cont{ istream_iterator<YString>{in}, {} };
	cout << endl;

	cont.sort([](const YString& a, const YString& b) {
		return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
		});

	for (const YString& ys : cont) {
		cout << ys << endl;
	}
}


-------------------------------------------------
저장시간 - 2026-05-06 11:39:45.6184678
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		5월 6일                                                             (10주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array -> random access(임의의 위치에 접근이 O(1))
// list<T> - 임의의 위치에서 추가/제거 동작이 O(1)
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <list>
#include <algorithm>
#include <fstream>
#include <vector>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	// [문제] 파일에 있는 단어를 list에 저장하라.
	// list를 사전기준으로 오름차순 정렬하라.
	// 화면에 출력하라

	ifstream in{ "2026 1학기 STL.txt" };
	if (not in) return 20260429;

	cout << "파일 읽는중";
	list<YString> cont{ istream_iterator<YString>{in}, {} };
	cout << endl;

	cont.sort([](const YString& a, const YString& b) {
		return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
		});
	
	cout << "읽은 단어 개수 - " << cont.size() << endl;

	// [문제] 같은 단어가 많아서 같은단어 다 없애면 모두 몇단어 인지 출력

	cont.unique([](const YString& a, const YString& b) {  // 인접(adjacent)한 원소끼리 비교
		return a == b;
		});
	for (const YString& ys : cont)
		cout << ys << endl;

	cout << "같은 단어 제거한 후 결과" << endl;
	cout << "단어 수 - " << cont.size() << endl;
}


-------------------------------------------------
저장시간 - 2026-05-06 11:56:23.2534586
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		5월 6일                                                             (10주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array -> random access(임의의 위치에 접근이 O(1))
// list<T> - 임의의 위치에서 추가/제거 동작이 O(1)
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <list>
#include <algorithm>
#include <fstream>
#include <vector>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	// [문제] 파일에 있는 단어를 list에 저장하라.
	// list를 사전기준으로 오름차순 정렬하라.
	// 화면에 출력하라

	ifstream in{ "2026 1학기 STL.txt" };
	if (not in) return 20260429;

	cout << "파일 읽는중";
	list<YString> cont{ istream_iterator<YString>{in}, {} };
	cout << endl;

	cont.sort([](const YString& a, const YString& b) {
		return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
		});
	
	cout << "읽은 단어 개수 - " << cont.size() << endl;

	// [문제] 같은 단어가 많아서 같은단어 다 없애면 모두 몇단어 인지 출력

	cont.unique([](const YString& a, const YString& b) {
		return a == b;
		});
	for (const YString& ys : cont)
		cout << ys << endl;

	cout << "같은 단어 제거한 후 결과" << endl;
	cout << "단어 수 - " << cont.size() << endl;

	// [문제] 사용자가 입력한 단어가 있는지 알려주자

	while (true) {
		cout << "찾을 단어는? ";
		YString word;
		cin >> word;

		// unique 실행 후 단어가 있다면 몇번째 단어인지 없으면 없는 단어라고 출력
		list<YString>::iterator p = find(cont.begin(), cont.end(), word);
		if (p == cont.end()) cout << "없는 단어" << endl;
		else cout << word << "는 " << distance(cont.begin(), p) + 1 << "번째 단어" << endl;
	}
}


-------------------------------------------------
저장시간 - 2026-05-06 12:09:47.4081603
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		5월 6일                                                             (10주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array -> random access(임의의 위치에 접근이 O(1))
// list<T> - 임의의 위치에서 추가/제거 동작이 O(1)
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <list>
#include <algorithm>
#include <fstream>
#include <vector>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	// [문제] 파일에 있는 단어를 list에 저장하라.
	// list를 사전기준으로 오름차순 정렬하라.
	// 화면에 출력하라

	ifstream in{ "2026 1학기 STL.txt" };
	if (not in) return 20260429;

	cout << "파일 읽는중";
	vector<YString> cont{ istream_iterator<YString>{in}, {} };
	cout << endl;

	sort(cont.begin(), cont.end(), [](const YString& a, const YString& b) {
		return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
		});
	
	cout << "읽은 단어 개수 - " << cont.size() << endl;

	// [문제] 같은 단어가 많아서 같은단어 다 없애면 모두 몇단어 인지 출력

	auto newEnd = unique(cont.begin(), cont.end(), [](const YString& a, const YString& b) {
		return a == b;
		});
	cont.erase(newEnd, cont.end());

	for (const YString& ys : cont)
		cout << ys << endl;

	cout << "같은 단어 제거한 후 결과" << endl;
	cout << "단어 수 - " << cont.size() << endl;

	// [문제] 사용자가 입력한 단어가 있는지 알려주자

	while (true) {
		cout << "찾을 단어는? ";
		YString word;
		cin >> word;

		// unique 실행 후 단어가 있다면 몇번째 단어인지 없으면 없는 단어라고 출력
		vector<YString>::iterator p = find(cont.begin(), cont.end(), word);
		if (p == cont.end()) cout << "없는 단어" << endl;
		else cout << word << "는 " << distance(p, cont.begin()) + 1 << "번째 단어" << endl;
	}
}


-------------------------------------------------
저장시간 - 2026-05-06 12:37:13.7992372
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		5월 6일                                                             (10주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array -> random access(임의의 위치에 접근이 O(1))
// list<T> - 임의의 위치에서 추가/제거 동작이 O(1)
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <list>
#include <algorithm>
#include <fstream>
#include <vector>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	// [문제] 파일에 있는 단어를 list에 저장하라.
	// list를 사전기준으로 오름차순 정렬하라.
	// 화면에 출력하라

	ifstream in{ "2026 1학기 STL.txt" };
	if (not in) return 20260429;

	cout << "파일 읽는중";
	vector<YString> cont;
	cont.reserve(50000);
	cont.assign(istream_iterator<YString>{in}, {});
	cout << endl;

	sort(cont.begin(), cont.end(), [](const YString& a, const YString& b) {
		return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
		});
	
	cout << "읽은 단어 개수 - " << cont.size() << endl;

	// [문제] 같은 단어가 많아서 같은단어 다 없애면 모두 몇단어 인지 출력

	auto newEnd = unique(cont.begin(), cont.end(), [](const YString& a, const YString& b) {
		return a == b;
		});
	cont.erase(newEnd, cont.end());

	for (const YString& ys : cont)
		cout << ys << endl;

	cout << "같은 단어 제거한 후 결과" << endl;
	cout << "단어 수 - " << cont.size() << endl;

	// [문제] 사용자가 입력한 단어가 있는지 알려주자

	while (true) {
		cout << "찾을 단어는? ";
		YString word;
		cin >> word;

		// unique 실행 후 단어가 있다면 몇번째 단어인지 없으면 없는 단어라고 출력
		vector<YString>::iterator p = find(cont.begin(), cont.end(), word);
		if (p == cont.end()) cout << "없는 단어" << endl;
		else cout << word << "는 " << distance(cont.begin(), p) + 1 << "번째 단어" << endl;
	}
}


-------------------------------------------------
저장시간 - 2026-05-06 12:41:06.0929871
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		5월 6일                                                             (10주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array -> random access(임의의 위치에 접근이 O(1))
// list<T> - 임의의 위치에서 추가/제거 동작이 O(1)
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <list>
#include <algorithm>
#include <fstream>
#include <vector>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	// [문제] 파일에 있는 단어를 list에 저장하라.
	// list를 사전기준으로 오름차순 정렬하라.
	// 화면에 출력하라

	ifstream in{ "2026 1학기 STL.txt" };
	if (not in) return 20260429;

	cout << "파일 읽는중";
	vector<YString> cont;
	cont.reserve(50000);
	cont.assign(istream_iterator<YString>{in}, {});
	cout << endl;

	sort(cont.begin(), cont.end(), [](const YString& a, const YString& b) {
		return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
		});
	
	cout << "읽은 단어 개수 - " << cont.size() << endl;

	// [문제] 같은 단어가 많아서 같은단어 다 없애면 모두 몇단어 인지 출력

	auto newEnd = unique(cont.begin(), cont.end(), [](const YString& a, const YString& b) {
		return a == b;
		});
	cont.erase(newEnd, cont.end());

	cout << "같은 단어 제거한 후 결과" << endl;
	cout << "단어 수 - " << cont.size() << endl;

	// [문제] 뒤에서 부터 10개를 show()
	관찰 = true;
	for (int i = 0; i < 10; ++i)
		(cont.end() - 1 - i)->show();
	관찰 = false;
}


-------------------------------------------------
저장시간 - 2026-05-06 12:45:29.9603867
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		5월 6일                                                             (10주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array -> random access(임의의 위치에 접근이 O(1))
// list<T> - 임의의 위치에서 추가/제거 동작이 O(1)
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <list>
#include <algorithm>
#include <fstream>
#include <vector>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	//save("메인.cpp");

	// [문제] 파일에 있는 단어를 list에 저장하라.
	// list를 사전기준으로 오름차순 정렬하라.
	// 화면에 출력하라

	ifstream in{ "2026 1학기 STL.txt" };
	if (not in) return 20260429;

	cout << "파일 읽는중";
	list<YString> cont;
	cont.assign(istream_iterator<YString>{in}, {});
	cout << endl;

	cont.sort([](const YString& a, const YString& b) {
		return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
		});
	
	cout << "읽은 단어 개수 - " << cont.size() << endl;

	// [문제] 같은 단어가 많아서 같은단어 다 없애면 모두 몇단어 인지 출력

	cont.unique([](const YString& a, const YString& b) {
		return a == b;
		});

	cout << "같은 단어 제거한 후 결과" << endl;
	cout << "단어 수 - " << cont.size() << endl;

	// [문제]뒤에서 부터 10개를 show()
	관찰 = true;
	for (int i = 0; i < 10; ++i)
		cont.rbegin()[i].show();
	관찰 = false;
}


-------------------------------------------------
저장시간 - 2026-05-06 12:48:28.3303841
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		5월 6일                                                             (10주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array -> random access(임의의 위치에 접근이 O(1))
// list<T> - 임의의 위치에서 추가/제거 동작이 O(1)
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <list>
#include <algorithm>
#include <fstream>
#include <vector>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	// [문제] 파일에 있는 단어를 list에 저장하라.
	// list를 사전기준으로 오름차순 정렬하라.
	// 화면에 출력하라

	ifstream in{ "2026 1학기 STL.txt" };
	if (not in) return 20260429;

	cout << "파일 읽는중";
	list<YString> cont;
	cont.assign(istream_iterator<YString>{in}, {});
	cout << endl;

	cont.sort([](const YString& a, const YString& b) {
		return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
		});
	
	cout << "읽은 단어 개수 - " << cont.size() << endl;

	// [문제] 같은 단어가 많아서 같은단어 다 없애면 모두 몇단어 인지 출력

	cont.unique([](const YString& a, const YString& b) {
		return a == b;
		});

	cout << "같은 단어 제거한 후 결과" << endl;
	cout << "단어 수 - " << cont.size() << endl;

	// [문제]뒤에서 부터 10개를 show()
	auto end = cont.end();
	관찰 = true;
	for (int i = 0; i< 10; ++i)
		(--end)->show();
	관찰 = false;
}


-------------------------------------------------
저장시간 - 2026-05-06 12:51:06.7141321
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		5월 6일                                                             (10주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - dynamic array -> random access(임의의 위치에 접근이 O(1))
// list<T> - 임의의 위치에서 추가/제거 동작이 O(1)
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <list>
#include <algorithm>
#include <fstream>
#include <vector>
#include <ranges>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	save("메인.cpp");

	// [문제] 파일에 있는 단어를 list에 저장하라.
	// list를 사전기준으로 오름차순 정렬하라.
	// 화면에 출력하라

	ifstream in{ "2026 1학기 STL.txt" };
	if (not in) return 20260429;

	cout << "파일 읽는중";
	list<YString> cont;
	cont.assign(istream_iterator<YString>{in}, {});
	cout << endl;

	cont.sort([](const YString& a, const YString& b) {
		return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
		});
	
	cout << "읽은 단어 개수 - " << cont.size() << endl;

	// [문제] 같은 단어가 많아서 같은단어 다 없애면 모두 몇단어 인지 출력

	cont.unique([](const YString& a, const YString& b) {
		return a == b;
		});

	cout << "같은 단어 제거한 후 결과" << endl;
	cout << "단어 수 - " << cont.size() << endl;

	// [문제]뒤에서 부터 10개를 show()
	관찰 = true;
	for (const YString& ys : cont | views::reverse | views::take(10))
		ys.show();
	관찰 = false;
}


-------------------------------------------------
저장시간 - 2026-05-06 13:08:38.5708804
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		5월 6일                                                             (10주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// Sequnce Container
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - 캐시 효율성 (Cache Locality)
// list<T> - 임의의 위치에서 추가/제거 동작이 O(1)
// deque<T> - vector와 list의 장점을 활용하려는 자료구조
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <deque>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	deque<int> numbers{ 1, 2, 3, 4 };

	// 원소의 메모리 위치를 확인한다 - contigous
	for (int i = 0; i < numbers.size(); ++i) 
		cout << addressof(numbers[i]) << endl;

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-05-06 13:10:31.8804772
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		5월 6일                                                             (10주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// Sequnce Container
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - 캐시 효율성 (Cache Locality)
// list<T> - 임의의 위치에서 추가/제거 동작이 O(1)
// deque<T> - vector와 list의 장점을 활용하려는 자료구조
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <deque>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	deque<int> numbers{ 1, 2, 3, 4 };

	// 원소의 메모리 위치를 확인한다 - contigous
	for (int i = 0; i < numbers.size(); ++i) 
		cout << addressof(numbers[i]) << endl;

	cout << "원소 한 개 추가" << endl;
	numbers.push_back(5);

	for (int i = 0; i < numbers.size(); ++i) 
		cout << addressof(numbers[i]) << endl;

	save("메인.cpp");
}


-------------------------------------------------
저장시간 - 2026-05-06 13:11:46.4844280
-------------------------------------------------

//------------------------------------------------------------------------------------------------------------
// 2026 1학기 STL 화56 수34		5월 6일                                                             (10주 2일)
//------------------------------------------------------------------------------------------------------------
// STL 컨테이너 - Containers are objects that store other objects
// Sequnce Container
// array<T, N> - 유일하게 크기 고정된 자료구조
// vector<T> - 캐시 효율성 (Cache Locality)
// list<T> - 임의의 위치에서 추가/제거 동작이 O(1)
// deque<T> - vector와 list의 장점을 활용하려는 자료구조
//------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <deque>
#include "save.h"
#include "YString.h"
using namespace std;

extern bool 관찰;

int main() {
	deque<int> numbers{ 1, 2, 3, 4 };

	// 원소의 메모리 위치를 확인한다 - contigous
	for (int i = 0; i < numbers.size(); ++i) 
		cout << addressof(numbers[i]) << endl;

	cout << "원소 한 개 추가" << endl;
	numbers.push_back(5);

	for (int i = 0; i < numbers.size(); ++i) 
		cout << addressof(numbers[i]) << endl;

	cout << "원소 한 개 추가" << endl;
	numbers.push_front(0);

	for (int i = 0; i < numbers.size(); ++i) 
		cout << addressof(numbers[i]) << endl;

	save("메인.cpp");
}

